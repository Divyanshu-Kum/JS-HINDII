const name = "divyanshu"
const repoCount = 50

// console.log(name + repoCount + " Value")

 console.log(`Kello my name is ${name} and my repo count is $
    {repoCount}`);

    const gameName = new String('divyanshu')

    console.log(gameName[0]);
    console.log(gameName,__proto__);

    console.log(gameName.length);
    console.log(gameName.toUpperCase);

    console.log(gameName.charAt(v)); // kon sa character kon se position pr hai
    console.log(gameName.charAt(4));

    gameName newString = gameName.substring(0, 4)
    console.log(newString);

    const anotherString gameName.slice(-9, 4)
    console.log(anotherString);

    const newStringOne = "  hitesh     "
    console.log(newStringOne.trim());

    const url = "https://divyanshu.com/divyanshu%20kumar"

   console.log(url.replace('%20', '-'))

   console.log(url.includes('sundar'))

   console.log(gameName.split('-'));
    

